const c=document.querySelector('#scene'),x=c.getContext('2d');let w,h,dots=[];function size(){w=c.width=innerWidth*devicePixelRatio;h=c.height=innerHeight*devicePixelRatio;x.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);dots=Array.from({length:Math.min(120,innerWidth/12)},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*1.5+.2,v:Math.random()*.16+.03,a:Math.random()*.4+.08}));}function draw(t){x.clearRect(0,0,innerWidth,innerHeight);let g=x.createRadialGradient(innerWidth*.68,innerHeight*.32,0,innerWidth*.68,innerHeight*.32,Math.max(innerWidth,innerHeight)*.65);g.addColorStop(0,'rgba(69,106,150,.20)');g.addColorStop(.35,'rgba(48,45,83,.12)');g.addColorStop(1,'rgba(8,7,13,0)');x.fillStyle=g;x.fillRect(0,0,innerWidth,innerHeight);for(const p of dots){p.y-=p.v;p.x+=Math.sin(t*.0002+p.y*.02)*.05;if(p.y<0)p.y=innerHeight;x.fillStyle=`rgba(134,188,204,${p.a})`;x.beginPath();x.arc(p.x,p.y,p.r,0,7);x.fill()}requestAnimationFrame(draw)}size();addEventListener('resize',size);requestAnimationFrame(draw);setInterval(()=>{document.querySelector('#coords').textContent=`GVA 46.20N 6.14E · UTC+1 ${new Date().toLocaleTimeString('fr-FR')}`},1000);document.querySelectorAll('.skill').forEach(b=>b.addEventListener('click',()=>b.classList.toggle('open')));
